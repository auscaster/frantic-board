# runx skill: answer from docs

This skill answers questions based on a documentation file.

## Usage

```bash
python skill.py "What is runx?"
```

The skill reads `docs.txt` and returns the most relevant paragraph.
